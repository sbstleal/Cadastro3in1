<UserControl x:Class="WpfAppCadastro.Views.PedidoView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:vm="clr-namespace:WpfAppCadastro.ViewModels"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="450" d:DesignWidth="900">
    <UserControl.DataContext>
        <vm:PedidoViewModel/>
    </UserControl.DataContext>

    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Formulário -->
        <StackPanel Orientation="Horizontal" Margin="0,0,0,10" VerticalAlignment="Top">
            <StackPanel Orientation="Vertical" Margin="0,0,10,0">
                <TextBlock Text="Cliente" />
                <TextBox Width="200" Text="{Binding PedidoSelecionado.Cliente, UpdateSourceTrigger=PropertyChanged}"/>
            </StackPanel>

            <StackPanel Orientation="Vertical" Margin="0,0,10,0">
                <TextBlock Text="Produto" />
                <TextBox Width="200" Text="{Binding PedidoSelecionado.Produto, UpdateSourceTrigger=PropertyChanged}"/>
            </StackPanel>

            <StackPanel Orientation="Vertical" Margin="0,0,10,0">
                <TextBlock Text="Quantidade" />
                <TextBox Width="100" Text="{Binding PedidoSelecionado.Quantidade, UpdateSourceTrigger=PropertyChanged}"/>
            </StackPanel>

            <StackPanel Orientation="Vertical" VerticalAlignment="Bottom" Margin="10,0,0,0">
                <Button Content="Adicionar" Command="{Binding AdicionarCommand}" Margin="0,0,0,5"/>
                <Button Content="Atualizar" Command="{Binding AtualizarCommand}" Margin="0,0,0,5"/>
                <Button Content="Remover" Command="{Binding RemoverCommand}" />
            </StackPanel>
        </StackPanel>

        <!-- Lista -->
        <DataGrid Grid.Row="1" ItemsSource="{Binding Pedidos}" SelectedItem="{Binding PedidoSelecionado}" AutoGenerateColumns="False">
            <DataGrid.Columns>
                <DataGridTextColumn Header="ID" Binding="{Binding Id}" Width="60"/>
                <DataGridTextColumn Header="Cliente" Binding="{Binding Cliente}" Width="*"/>
                <DataGridTextColumn Header="Produto" Binding="{Binding Produto}" Width="*"/>
                <DataGridTextColumn Header="Quantidade" Binding="{Binding Quantidade}" Width="100"/>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>