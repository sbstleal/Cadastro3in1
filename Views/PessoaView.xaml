<Window x:Class="WpfAppCadastro.Views.PessoaView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:vm="clr-namespace:WpfAppCadastro.ViewModels"
        Title="Cadastro de Pessoas" Height="450" Width="800">

    <Window.DataContext>
        <vm:PessoaViewModel/>
    </Window.DataContext>

    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Formulário -->
        <StackPanel Orientation="Horizontal" Margin="0,0,0,10">
            <TextBox Width="200" Margin="5" PlaceholderText="Nome" Text="{Binding PessoaSelecionada.Nome, UpdateSourceTrigger=PropertyChanged}"/>
            <TextBox Width="200" Margin="5" PlaceholderText="Email" Text="{Binding PessoaSelecionada.Email, UpdateSourceTrigger=PropertyChanged}"/>
            <Button Content="Adicionar" Command="{Binding AdicionarCommand}" Margin="5"/>
            <Button Content="Atualizar" Command="{Binding AtualizarCommand}" Margin="5"/>
            <Button Content="Remover" Command="{Binding RemoverCommand}" Margin="5"/>
        </StackPanel>

        <!-- Lista -->
        <DataGrid Grid.Row="1" ItemsSource="{Binding Pessoas}" SelectedItem="{Binding PessoaSelecionada}" AutoGenerateColumns="False">
            <DataGrid.Columns>
                <DataGridTextColumn Header="ID" Binding="{Binding Id}" Width="50"/>
                <DataGridTextColumn Header="Nome" Binding="{Binding Nome}" Width="*"/>
                <DataGridTextColumn Header="Email" Binding="{Binding Email}" Width="*"/>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</Window>